Update the .ai/context/ files to reflect the current codebase.

Context = current state + pending uncommitted work. For future work, reference .ai/plans/ (e.g. "Plan A steps 1–3 in progress; see plan_a.md for steps 4–5 on deck").

1. Read context_base.md and context_extended_toc.md
2. Scan the codebase for changes since last update
3. Update context_extended/*.md files as needed:
   - architecture.md — data flow, state, concurrency
   - game_engine.md — GameManager, modes, methods
   - api_routes.md — endpoints, request/response
   - ai_players.md — Player abstraction, registry, implementations
   - frontend.md — components, flows
   - storage.md — PGN, SQLite, replay
   - scripts_tooling.md — bat files, tooling

4. Update context_base.md if key paths or overview changed

Keep context concise. No code blocks unless essential.
